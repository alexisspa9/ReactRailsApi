{
  "name": "list-of-ingredients",
  "license": "MIT",
  "engines": {
    "node": "10.15.3",
     "yarn": "1.15.2"
  },
  "scripts": {
    "build": "npm install --prefix client && npm build --prefix client",
    "deploy": "cp -a client/build/. public/",
    "heroku-postbuild": "yarn build && yarn deploy"
  }
}